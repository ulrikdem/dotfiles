#!/bin/bash
prefix=()
if [[ ! -v SANDBOX ]]; then
    prefix=(sandbox -xRB 'org.pwmt.zathura.*')
    if [[ $1 = -n ]]; then
        prefix+=(-r "${5%pdf}synctex.gz" -r ~/.cache/vimtex/nvim_servernames.log -w "$2")
        shift 2
    fi
fi
exec "${prefix[@]}" /usr/bin/zathura "$@"
