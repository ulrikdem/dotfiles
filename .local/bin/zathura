#!/bin/bash
args=(-xRB 'org.pwmt.zathura.*')
if [[ $1 = -n ]]; then
    args+=(-r "${5%pdf}synctex.gz" -r ~/.cache/vimtex/nvim_servernames.log -w "$2")
    shift 2
fi
exec sandbox "${args[@]}" /usr/bin/zathura "$@"
