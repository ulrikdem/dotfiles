#!/bin/bash
temp=$(mktemp -t unicode.XXXXXXXX)
alacritty --class xmonad-custom-float -t unicode -e unicode-input-helper "$temp"
xdotool type -- "$(<"$temp")"
xsel <"$temp"
xsel -b <"$temp"
rm -- "$temp"
